<td rowspan = "2"><?= "Test" ?></td>

avec colspan pour le sous total           
           
            <?php if (empty($resultatRecherche) && $rechercheJeu !== "") { ?> 




             if (in_array($subject, $optionsSelect)) {
        $errorMsg[] = "Veuillez sélectionner un sujet valide dans la liste déroulante";
    };